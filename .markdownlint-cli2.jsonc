// markdownlint-cli2 configuration
{
  "config": {
    // Disable line length check - we have long URLs and code examples
    "MD013": false,

    // Allow inline HTML - needed for generic type parameters like <T>
    "MD033": false
  },

  "globs": [
    "**/*.md"
  ],

  "ignores": [
    "node_modules",
    "target",
    ".git"
  ]
}
