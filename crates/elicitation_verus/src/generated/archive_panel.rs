// AUTO-GENERATED by `elicitation generate verus` — DO NOT EDIT
//
// Standalone Verus proof for ArchivePanelMachine (V13 assume_specification pattern).
// Regenerate: elicitation generate verus --crate-path <root>
//
// No imports from the VSM crate — self-contained for vargo builds.
// Architecture:
//   V11/V12 leaf + composition proof grounded by V13 assume_specification.
//   Each stub is trusted axiomatically (requires/ensures mirrors the
//   formal_method contract); Kani and Creusot independently verify the
//   real transition bodies in elicit_server.

use vstd::prelude::*;
use verus_builtin_macros::verus;

// ─── External transition stubs ─────────────────────────────────────────────────
// These functions represent the real transitions in elicit_server.
// Verus does not verify their bodies; assume_specification below injects
// the trusted contracts.  Kani/Creusot independently verify the real bodies.

/// Stub for `column_detail` — body is opaque to Verus.
#[verifier::external]
pub fn column_detail_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `panel_loading` — body is opaque to Verus.
#[verifier::external]
pub fn panel_loading_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `panel_error` — body is opaque to Verus.
#[verifier::external]
pub fn panel_error_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `data_grid_ready` — body is opaque to Verus.
#[verifier::external]
pub fn data_grid_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `query_complete` — body is opaque to Verus.
#[verifier::external]
pub fn query_complete_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `begin_edit` — body is opaque to Verus.
#[verifier::external]
pub fn begin_edit_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `commit_edits` — body is opaque to Verus.
#[verifier::external]
pub fn commit_edits_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `abort_edits` — body is opaque to Verus.
#[verifier::external]
pub fn abort_edits_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `open_sql_editor` — body is opaque to Verus.
#[verifier::external]
pub fn open_sql_editor_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `open_export_panel` — body is opaque to Verus.
#[verifier::external]
pub fn open_export_panel_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `open_help_panel` — body is opaque to Verus.
#[verifier::external]
pub fn open_help_panel_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `open_saved_panel` — body is opaque to Verus.
#[verifier::external]
pub fn open_saved_panel_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `open_connection_editor` — body is opaque to Verus.
#[verifier::external]
pub fn open_connection_editor_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `ddl_ready` — body is opaque to Verus.
#[verifier::external]
pub fn ddl_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `explain_ready` — body is opaque to Verus.
#[verifier::external]
pub fn explain_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `export_ready` — body is opaque to Verus.
#[verifier::external]
pub fn export_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `history_ready` — body is opaque to Verus.
#[verifier::external]
pub fn history_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `saved_ready` — body is opaque to Verus.
#[verifier::external]
pub fn saved_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `monitor_ready` — body is opaque to Verus.
#[verifier::external]
pub fn monitor_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `admin_ready` — body is opaque to Verus.
#[verifier::external]
pub fn admin_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `erd_ready` — body is opaque to Verus.
#[verifier::external]
pub fn erd_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `constraints_ready` — body is opaque to Verus.
#[verifier::external]
pub fn constraints_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

/// Stub for `indexes_ready` — body is opaque to Verus.
#[verifier::external]
pub fn indexes_ready_stub(state: ArchivePanelState) -> ArchivePanelState { todo!() }

verus! {

/// Abstract mirror of `ArchivePanelState` (invariant-relevant variants only).
#[allow(unused_imports)]
use vstd::prelude::SpecOrd;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ArchivePanelState {
    SqlEditor { running: bool, result: Option<u64> },
    _Other,
}

/// Invariant for `ArchivePanelMachine`.
pub open spec fn archive_panel_consistent(state: &ArchivePanelState) -> bool { match *state { ArchivePanelState::SqlEditor { running, result, .. } => running ==> result.is_None(), _ => true, } }

// ─── Postcondition predicates ──────────────────────────────────────────────────

/// Any non-special post-state — the `_ => true` arm of the invariant applies.
pub open spec fn archive_panel_post_trivial(post: ArchivePanelState) -> bool { !(post matches ArchivePanelState::SqlEditor { .. }) }

/// Passthrough: post-state equals pre-state.
pub open spec fn archive_panel_post_passthrough(pre: ArchivePanelState, post: ArchivePanelState) -> bool { post == pre }

/// `SqlEditor` post with non-violating field (invariant vacuously satisfied).
pub open spec fn archive_panel_post_sql_editor_false(post: ArchivePanelState) -> bool {
    match post {
        ArchivePanelState::SqlEditor { running, result, .. } => running ==> result.is_None(),
        _ => false,
    }
}

/// Conditional: `SqlEditor` with non-violating field, or unchanged passthrough.
pub open spec fn archive_panel_post_conditional_sql_editor(pre: ArchivePanelState, post: ArchivePanelState) -> bool {
    match pre {
        ArchivePanelState::SqlEditor { .. } => match post {
            ArchivePanelState::SqlEditor { running, result, .. } => running ==> result.is_None(),
            _ => false,
        },
        _ => post == pre,
    }
}

// ─── Leaf lemmas ────────────────────────────────────────────────────────────────

/// Trivial post satisfies the invariant (`_ => true` arm applies).
pub proof fn archive_panel_leaf_trivial(post: ArchivePanelState)
    requires archive_panel_post_trivial(post),
    ensures  archive_panel_consistent(&post),
{}

/// Passthrough preserves the invariant by induction.
pub proof fn archive_panel_leaf_passthrough(pre: ArchivePanelState, post: ArchivePanelState)
    requires
        archive_panel_consistent(&pre),
        archive_panel_post_passthrough(pre, post),
    ensures archive_panel_consistent(&post),
{}

/// `SqlEditor` with non-violating field — invariant is vacuously satisfied.
pub proof fn archive_panel_leaf_sql_editor_false(post: ArchivePanelState)
    requires archive_panel_post_sql_editor_false(post),
    ensures  archive_panel_consistent(&post),
{}

/// Conditional `SqlEditor` — non-violating or passthrough — satisfies the invariant.
pub proof fn archive_panel_leaf_conditional_sql_editor(pre: ArchivePanelState, post: ArchivePanelState)
    requires
        archive_panel_consistent(&pre),
        archive_panel_post_conditional_sql_editor(pre, post),
    ensures archive_panel_consistent(&post),
{}

// ─── Assume specifications (V13 trust anchors) ──────────────────────────────────

// Each assume_specification axiomatizes the contract for its stub.
// The postcondition predicate is the same one used by the leaf lemma above,
// ensuring Verus proof and assume_specification are tightly aligned.

/// Contract for `column_detail_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `column_detail` body.
pub assume_specification[column_detail_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `panel_loading_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `panel_loading` body.
pub assume_specification[panel_loading_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `panel_error_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `panel_error` body.
pub assume_specification[panel_error_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `data_grid_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `data_grid_ready` body.
pub assume_specification[data_grid_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `query_complete_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `query_complete` body.
pub assume_specification[query_complete_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_conditional_sql_editor(state, r);

/// Contract for `begin_edit_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `begin_edit` body.
pub assume_specification[begin_edit_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_passthrough(state, r);

/// Contract for `commit_edits_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `commit_edits` body.
pub assume_specification[commit_edits_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_passthrough(state, r);

/// Contract for `abort_edits_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `abort_edits` body.
pub assume_specification[abort_edits_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `open_sql_editor_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_sql_editor` body.
pub assume_specification[open_sql_editor_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_sql_editor_false(r);

/// Contract for `open_export_panel_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_export_panel` body.
pub assume_specification[open_export_panel_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `open_help_panel_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_help_panel` body.
pub assume_specification[open_help_panel_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `open_saved_panel_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_saved_panel` body.
pub assume_specification[open_saved_panel_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `open_connection_editor_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_connection_editor` body.
pub assume_specification[open_connection_editor_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `ddl_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `ddl_ready` body.
pub assume_specification[ddl_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `explain_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `explain_ready` body.
pub assume_specification[explain_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_passthrough(state, r);

/// Contract for `export_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `export_ready` body.
pub assume_specification[export_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_passthrough(state, r);

/// Contract for `history_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `history_ready` body.
pub assume_specification[history_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `saved_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `saved_ready` body.
pub assume_specification[saved_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `monitor_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `monitor_ready` body.
pub assume_specification[monitor_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `admin_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `admin_ready` body.
pub assume_specification[admin_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `erd_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `erd_ready` body.
pub assume_specification[erd_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `constraints_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `constraints_ready` body.
pub assume_specification[constraints_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

/// Contract for `indexes_ready_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `indexes_ready` body.
pub assume_specification[indexes_ready_stub](state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_post_trivial(r);

// ─── Verified exec callers ───────────────────────────────────────────────────────

// These exec fns close the proof loop: calling any stub on a consistent
// pre-state yields a consistent post-state, by the assume_specification.

/// Proof that `column_detail` preserves the invariant (via assume_specification).
pub fn column_detail_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    column_detail_stub(state)
}

/// Proof that `panel_loading` preserves the invariant (via assume_specification).
pub fn panel_loading_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    panel_loading_stub(state)
}

/// Proof that `panel_error` preserves the invariant (via assume_specification).
pub fn panel_error_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    panel_error_stub(state)
}

/// Proof that `data_grid_ready` preserves the invariant (via assume_specification).
pub fn data_grid_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    data_grid_ready_stub(state)
}

/// Proof that `query_complete` preserves the invariant (via assume_specification).
pub fn query_complete_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    query_complete_stub(state)
}

/// Proof that `begin_edit` preserves the invariant (via assume_specification).
pub fn begin_edit_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    begin_edit_stub(state)
}

/// Proof that `commit_edits` preserves the invariant (via assume_specification).
pub fn commit_edits_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    commit_edits_stub(state)
}

/// Proof that `abort_edits` preserves the invariant (via assume_specification).
pub fn abort_edits_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    abort_edits_stub(state)
}

/// Proof that `open_sql_editor` preserves the invariant (via assume_specification).
pub fn open_sql_editor_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    open_sql_editor_stub(state)
}

/// Proof that `open_export_panel` preserves the invariant (via assume_specification).
pub fn open_export_panel_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    open_export_panel_stub(state)
}

/// Proof that `open_help_panel` preserves the invariant (via assume_specification).
pub fn open_help_panel_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    open_help_panel_stub(state)
}

/// Proof that `open_saved_panel` preserves the invariant (via assume_specification).
pub fn open_saved_panel_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    open_saved_panel_stub(state)
}

/// Proof that `open_connection_editor` preserves the invariant (via assume_specification).
pub fn open_connection_editor_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    open_connection_editor_stub(state)
}

/// Proof that `ddl_ready` preserves the invariant (via assume_specification).
pub fn ddl_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    ddl_ready_stub(state)
}

/// Proof that `explain_ready` preserves the invariant (via assume_specification).
pub fn explain_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    explain_ready_stub(state)
}

/// Proof that `export_ready` preserves the invariant (via assume_specification).
pub fn export_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    export_ready_stub(state)
}

/// Proof that `history_ready` preserves the invariant (via assume_specification).
pub fn history_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    history_ready_stub(state)
}

/// Proof that `saved_ready` preserves the invariant (via assume_specification).
pub fn saved_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    saved_ready_stub(state)
}

/// Proof that `monitor_ready` preserves the invariant (via assume_specification).
pub fn monitor_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    monitor_ready_stub(state)
}

/// Proof that `admin_ready` preserves the invariant (via assume_specification).
pub fn admin_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    admin_ready_stub(state)
}

/// Proof that `erd_ready` preserves the invariant (via assume_specification).
pub fn erd_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    erd_ready_stub(state)
}

/// Proof that `constraints_ready` preserves the invariant (via assume_specification).
pub fn constraints_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    constraints_ready_stub(state)
}

/// Proof that `indexes_ready` preserves the invariant (via assume_specification).
pub fn indexes_ready_verified(state: ArchivePanelState) -> (r: ArchivePanelState)
    requires archive_panel_consistent(&state),
    ensures  archive_panel_consistent(&r),
{
    indexes_ready_stub(state)
}

// ─── Transition tags + composition ──────────────────────────────────────────────

/// One tag per `ArchivePanelMachine` transition — dispatch without re-verifying bodies.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ArchivePanelMachineTrans {
    ColumnDetail,
    PanelLoading,
    PanelError,
    DataGridReady,
    QueryComplete,
    BeginEdit,
    CommitEdits,
    AbortEdits,
    OpenSqlEditor,
    OpenExportPanel,
    OpenHelpPanel,
    OpenSavedPanel,
    OpenConnectionEditor,
    DdlReady,
    ExplainReady,
    ExportReady,
    HistoryReady,
    SavedReady,
    MonitorReady,
    AdminReady,
    ErdReady,
    ConstraintsReady,
    IndexesReady,
}

/// Maps each transition tag to its postcondition predicate.
pub open spec fn archive_panel_post(pre: ArchivePanelState, post: ArchivePanelState, tag: ArchivePanelMachineTrans) -> bool {
    match tag {
        ArchivePanelMachineTrans::ColumnDetail => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::PanelLoading => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::PanelError => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::DataGridReady => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::QueryComplete => archive_panel_post_conditional_sql_editor(pre, post),
        ArchivePanelMachineTrans::BeginEdit => archive_panel_post_passthrough(pre, post),
        ArchivePanelMachineTrans::CommitEdits => archive_panel_post_passthrough(pre, post),
        ArchivePanelMachineTrans::AbortEdits => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::OpenSqlEditor => archive_panel_post_sql_editor_false(post),
        ArchivePanelMachineTrans::OpenExportPanel => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::OpenHelpPanel => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::OpenSavedPanel => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::OpenConnectionEditor => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::DdlReady => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::ExplainReady => archive_panel_post_passthrough(pre, post),
        ArchivePanelMachineTrans::ExportReady => archive_panel_post_passthrough(pre, post),
        ArchivePanelMachineTrans::HistoryReady => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::SavedReady => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::MonitorReady => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::AdminReady => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::ErdReady => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::ConstraintsReady => archive_panel_post_trivial(post),
        ArchivePanelMachineTrans::IndexesReady => archive_panel_post_trivial(post),
    }
}

/// Composition: any post-state produced by a tagged transition satisfies the invariant.
pub proof fn archive_panel_composition(pre: ArchivePanelState, post: ArchivePanelState, tag: ArchivePanelMachineTrans)
    requires
        archive_panel_consistent(&pre),
        archive_panel_post(pre, post, tag),
    ensures archive_panel_consistent(&post),
{
    match tag {
        ArchivePanelMachineTrans::ColumnDetail => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::PanelLoading => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::PanelError => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::DataGridReady => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::QueryComplete => archive_panel_leaf_conditional_sql_editor(pre, post),
        ArchivePanelMachineTrans::BeginEdit => archive_panel_leaf_passthrough(pre, post),
        ArchivePanelMachineTrans::CommitEdits => archive_panel_leaf_passthrough(pre, post),
        ArchivePanelMachineTrans::AbortEdits => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::OpenSqlEditor => archive_panel_leaf_sql_editor_false(post),
        ArchivePanelMachineTrans::OpenExportPanel => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::OpenHelpPanel => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::OpenSavedPanel => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::OpenConnectionEditor => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::DdlReady => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::ExplainReady => archive_panel_leaf_passthrough(pre, post),
        ArchivePanelMachineTrans::ExportReady => archive_panel_leaf_passthrough(pre, post),
        ArchivePanelMachineTrans::HistoryReady => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::SavedReady => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::MonitorReady => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::AdminReady => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::ErdReady => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::ConstraintsReady => archive_panel_leaf_trivial(post),
        ArchivePanelMachineTrans::IndexesReady => archive_panel_leaf_trivial(post),
    }
}

} // verus!
