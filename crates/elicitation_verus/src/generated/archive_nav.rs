// AUTO-GENERATED by `elicitation generate verus` — DO NOT EDIT
//
// Standalone Verus proof for ArchiveNavMachine (V13 assume_specification pattern).
// Regenerate: elicitation generate verus --crate-path <root>
//
// No imports from the VSM crate — self-contained for vargo builds.
// Architecture:
//   V11/V12 leaf + composition proof grounded by V13 assume_specification.
//   Each stub is trusted axiomatically (requires/ensures mirrors the
//   formal_method contract); Kani and Creusot independently verify the
//   real transition bodies in elicit_server.

use verus_builtin_macros::verus;

// ─── External transition stubs ─────────────────────────────────────────────────
// These functions represent the real transitions in elicit_server.
// Verus does not verify their bodies; assume_specification below injects
// the trusted contracts.  Kani/Creusot independently verify the real bodies.
verus! {

/// Stub for `load_nav` — body is opaque to Verus.
#[verifier::external]
pub fn load_nav_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }

/// Stub for `nav_loaded` — body is opaque to Verus.
#[verifier::external]
pub fn nav_loaded_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }

/// Stub for `nav_refresh` — body is opaque to Verus.
#[verifier::external]
pub fn nav_refresh_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }

/// Stub for `expand_schema` — body is opaque to Verus.
#[verifier::external]
pub fn expand_schema_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }

/// Stub for `collapse_schema` — body is opaque to Verus.
#[verifier::external]
pub fn collapse_schema_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }

/// Stub for `move_cursor_up` — body is opaque to Verus.
#[verifier::external]
pub fn move_cursor_up_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }

/// Stub for `move_cursor_down` — body is opaque to Verus.
#[verifier::external]
pub fn move_cursor_down_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }

/// Stub for `apply_filter` — body is opaque to Verus.
#[verifier::external]
pub fn apply_filter_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }

/// Stub for `clear_filter` — body is opaque to Verus.
#[verifier::external]
pub fn clear_filter_stub(_state: ArchiveNavState) -> ArchiveNavState { todo!() }


#[allow(unused_imports)]
use vstd::prelude::*;
#[allow(unused_imports)]
use vstd::prelude::SpecOrd;

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum ArchiveNavState {
    NavFiltered { filter: String },
    _Other,
}

/// Invariant for `ArchiveNavMachine`.
pub open spec fn archive_nav_consistent(state: &ArchiveNavState) -> bool { match *state { ArchiveNavState::NavFiltered { filter, .. } => filter@.len() > 0, _ => true, } }

// ─── Postcondition predicates ──────────────────────────────────────────────────

/// Any non-special post-state — the `_ => true` arm of the invariant applies.
pub open spec fn archive_nav_post_trivial(post: ArchiveNavState) -> bool { !(post matches ArchiveNavState::NavFiltered { .. }) }

/// Passthrough: post-state equals pre-state.
pub open spec fn archive_nav_post_passthrough(pre: ArchiveNavState, post: ArchiveNavState) -> bool { post == pre }

/// Conditional: `NavFiltered` with non-violating field, or unchanged passthrough.
pub open spec fn archive_nav_post_conditional_nav_filtered(pre: ArchiveNavState, post: ArchiveNavState) -> bool {
    match pre {
        ArchiveNavState::NavFiltered { .. } => match post {
            ArchiveNavState::NavFiltered { filter, .. } => filter@.len() > 0,
            _ => false,
        },
        _ => post == pre,
    }
}

// ─── Leaf lemmas ────────────────────────────────────────────────────────────────

/// Trivial post satisfies the invariant (`_ => true` arm applies).
pub proof fn archive_nav_leaf_trivial(post: ArchiveNavState)
    requires archive_nav_post_trivial(post),
    ensures  archive_nav_consistent(&post),
{}

/// Passthrough preserves the invariant by induction.
pub proof fn archive_nav_leaf_passthrough(pre: ArchiveNavState, post: ArchiveNavState)
    requires
        archive_nav_consistent(&pre),
        archive_nav_post_passthrough(pre, post),
    ensures archive_nav_consistent(&post),
{}

/// Conditional `NavFiltered` — non-violating or passthrough — satisfies the invariant.
pub proof fn archive_nav_leaf_conditional_nav_filtered(pre: ArchiveNavState, post: ArchiveNavState)
    requires
        archive_nav_consistent(&pre),
        archive_nav_post_conditional_nav_filtered(pre, post),
    ensures archive_nav_consistent(&post),
{}

// ─── Assume specifications (V13 trust anchors) ──────────────────────────────────

// Each assume_specification axiomatizes the contract for its stub.
// The postcondition predicate is the same one used by the leaf lemma above,
// ensuring Verus proof and assume_specification are tightly aligned.

/// Contract for `load_nav_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `load_nav` body.
pub assume_specification[load_nav_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_trivial(r);

/// Contract for `nav_loaded_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `nav_loaded` body.
pub assume_specification[nav_loaded_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_trivial(r);

/// Contract for `nav_refresh_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `nav_refresh` body.
pub assume_specification[nav_refresh_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_trivial(r);

/// Contract for `expand_schema_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `expand_schema` body.
pub assume_specification[expand_schema_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_passthrough(state, r);

/// Contract for `collapse_schema_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `collapse_schema` body.
pub assume_specification[collapse_schema_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_trivial(r);

/// Contract for `move_cursor_up_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `move_cursor_up` body.
pub assume_specification[move_cursor_up_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_passthrough(state, r);

/// Contract for `move_cursor_down_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `move_cursor_down` body.
pub assume_specification[move_cursor_down_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_passthrough(state, r);

/// Contract for `apply_filter_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `apply_filter` body.
pub assume_specification[apply_filter_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_conditional_nav_filtered(state, r);

/// Contract for `clear_filter_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `clear_filter` body.
pub assume_specification[clear_filter_stub](state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_post_trivial(r);

// ─── Verified exec callers ───────────────────────────────────────────────────────

// These exec fns close the proof loop: calling any stub on a consistent
// pre-state yields a consistent post-state, by the assume_specification.

/// Proof that `load_nav` preserves the invariant (via assume_specification).
pub fn load_nav_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    load_nav_stub(state)
}

/// Proof that `nav_loaded` preserves the invariant (via assume_specification).
pub fn nav_loaded_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    nav_loaded_stub(state)
}

/// Proof that `nav_refresh` preserves the invariant (via assume_specification).
pub fn nav_refresh_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    nav_refresh_stub(state)
}

/// Proof that `expand_schema` preserves the invariant (via assume_specification).
pub fn expand_schema_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    expand_schema_stub(state)
}

/// Proof that `collapse_schema` preserves the invariant (via assume_specification).
pub fn collapse_schema_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    collapse_schema_stub(state)
}

/// Proof that `move_cursor_up` preserves the invariant (via assume_specification).
pub fn move_cursor_up_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    move_cursor_up_stub(state)
}

/// Proof that `move_cursor_down` preserves the invariant (via assume_specification).
pub fn move_cursor_down_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    move_cursor_down_stub(state)
}

/// Proof that `apply_filter` preserves the invariant (via assume_specification).
pub fn apply_filter_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    apply_filter_stub(state)
}

/// Proof that `clear_filter` preserves the invariant (via assume_specification).
pub fn clear_filter_verified(state: ArchiveNavState) -> (r: ArchiveNavState)
    requires archive_nav_consistent(&state),
    ensures  archive_nav_consistent(&r),
{
    clear_filter_stub(state)
}

// ─── Transition tags + composition ──────────────────────────────────────────────

/// One tag per `ArchiveNavMachine` transition — dispatch without re-verifying bodies.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ArchiveNavMachineTrans {
    LoadNav,
    NavLoaded,
    NavRefresh,
    ExpandSchema,
    CollapseSchema,
    MoveCursorUp,
    MoveCursorDown,
    ApplyFilter,
    ClearFilter,
}

/// Maps each transition tag to its postcondition predicate.
pub open spec fn archive_nav_post(pre: ArchiveNavState, post: ArchiveNavState, tag: ArchiveNavMachineTrans) -> bool {
    match tag {
        ArchiveNavMachineTrans::LoadNav => archive_nav_post_trivial(post),
        ArchiveNavMachineTrans::NavLoaded => archive_nav_post_trivial(post),
        ArchiveNavMachineTrans::NavRefresh => archive_nav_post_trivial(post),
        ArchiveNavMachineTrans::ExpandSchema => archive_nav_post_passthrough(pre, post),
        ArchiveNavMachineTrans::CollapseSchema => archive_nav_post_trivial(post),
        ArchiveNavMachineTrans::MoveCursorUp => archive_nav_post_passthrough(pre, post),
        ArchiveNavMachineTrans::MoveCursorDown => archive_nav_post_passthrough(pre, post),
        ArchiveNavMachineTrans::ApplyFilter => archive_nav_post_conditional_nav_filtered(pre, post),
        ArchiveNavMachineTrans::ClearFilter => archive_nav_post_trivial(post),
    }
}

/// Composition: any post-state produced by a tagged transition satisfies the invariant.
pub proof fn archive_nav_composition(pre: ArchiveNavState, post: ArchiveNavState, tag: ArchiveNavMachineTrans)
    requires
        archive_nav_consistent(&pre),
        archive_nav_post(pre, post, tag),
    ensures archive_nav_consistent(&post),
{
    match tag {
        ArchiveNavMachineTrans::LoadNav => archive_nav_leaf_trivial(post),
        ArchiveNavMachineTrans::NavLoaded => archive_nav_leaf_trivial(post),
        ArchiveNavMachineTrans::NavRefresh => archive_nav_leaf_trivial(post),
        ArchiveNavMachineTrans::ExpandSchema => archive_nav_leaf_passthrough(pre, post),
        ArchiveNavMachineTrans::CollapseSchema => archive_nav_leaf_trivial(post),
        ArchiveNavMachineTrans::MoveCursorUp => archive_nav_leaf_passthrough(pre, post),
        ArchiveNavMachineTrans::MoveCursorDown => archive_nav_leaf_passthrough(pre, post),
        ArchiveNavMachineTrans::ApplyFilter => archive_nav_leaf_conditional_nav_filtered(pre, post),
        ArchiveNavMachineTrans::ClearFilter => archive_nav_leaf_trivial(post),
    }
}

} // verus!
