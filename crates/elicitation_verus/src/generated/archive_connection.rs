// AUTO-GENERATED by `elicitation generate verus` — DO NOT EDIT
//
// Standalone Verus proof for ArchiveConnectionMachine (V13 assume_specification pattern).
// Regenerate: elicitation generate verus --crate-path <root>
//
// No imports from the VSM crate — self-contained for vargo builds.
// Architecture:
//   V11/V12 leaf + composition proof grounded by V13 assume_specification.
//   Each stub is trusted axiomatically (requires/ensures mirrors the
//   formal_method contract); Kani and Creusot independently verify the
//   real transition bodies in elicit_server.

use vstd::prelude::*;
use verus_builtin_macros::verus;

// ─── External transition stubs ─────────────────────────────────────────────────
// These functions represent the real transitions in elicit_server.
// Verus does not verify their bodies; assume_specification below injects
// the trusted contracts.  Kani/Creusot independently verify the real bodies.

/// Stub for `begin_connect_sql` — body is opaque to Verus.
#[verifier::external]
pub fn begin_connect_sql_stub(state: ArchiveConnectionState) -> ArchiveConnectionState { todo!() }

/// Stub for `begin_connect_kv` — body is opaque to Verus.
#[verifier::external]
pub fn begin_connect_kv_stub(state: ArchiveConnectionState) -> ArchiveConnectionState { todo!() }

/// Stub for `finish_connect_sql` — body is opaque to Verus.
#[verifier::external]
pub fn finish_connect_sql_stub(state: ArchiveConnectionState) -> ArchiveConnectionState { todo!() }

/// Stub for `finish_connect_kv` — body is opaque to Verus.
#[verifier::external]
pub fn finish_connect_kv_stub(state: ArchiveConnectionState) -> ArchiveConnectionState { todo!() }

/// Stub for `disconnect` — body is opaque to Verus.
#[verifier::external]
pub fn disconnect_stub(state: ArchiveConnectionState) -> ArchiveConnectionState { todo!() }

/// Stub for `reconnect` — body is opaque to Verus.
#[verifier::external]
pub fn reconnect_stub(state: ArchiveConnectionState) -> ArchiveConnectionState { todo!() }

/// Stub for `connection_error` — body is opaque to Verus.
#[verifier::external]
pub fn connection_error_stub(state: ArchiveConnectionState) -> ArchiveConnectionState { todo!() }

verus! {

/// Abstract mirror of `ArchiveConnectionState` (invariant-relevant variants only).
#[allow(unused_imports)]
use vstd::prelude::SpecOrd;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ArchiveConnectionState {
    _Unspecified,
}

/// Invariant for `ArchiveConnectionMachine`.
pub open spec fn archive_connection_consistent(state: &ArchiveConnectionState) -> bool { true }

// ─── Postcondition predicates ──────────────────────────────────────────────────

/// Any non-special post-state — the `_ => true` arm of the invariant applies.
pub open spec fn archive_connection_post_trivial(post: ArchiveConnectionState) -> bool { true }

/// Passthrough: post-state equals pre-state.
pub open spec fn archive_connection_post_passthrough(pre: ArchiveConnectionState, post: ArchiveConnectionState) -> bool { post == pre }

// ─── Leaf lemmas ────────────────────────────────────────────────────────────────

/// Trivial post satisfies the invariant (`_ => true` arm applies).
pub proof fn archive_connection_leaf_trivial(post: ArchiveConnectionState)
    requires archive_connection_post_trivial(post),
    ensures  archive_connection_consistent(&post),
{}

/// Passthrough preserves the invariant by induction.
pub proof fn archive_connection_leaf_passthrough(pre: ArchiveConnectionState, post: ArchiveConnectionState)
    requires
        archive_connection_consistent(&pre),
        archive_connection_post_passthrough(pre, post),
    ensures archive_connection_consistent(&post),
{}

// ─── Assume specifications (V13 trust anchors) ──────────────────────────────────

// Each assume_specification axiomatizes the contract for its stub.
// The postcondition predicate is the same one used by the leaf lemma above,
// ensuring Verus proof and assume_specification are tightly aligned.

/// Contract for `begin_connect_sql_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `begin_connect_sql` body.
pub assume_specification[begin_connect_sql_stub](state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_post_trivial(r);

/// Contract for `begin_connect_kv_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `begin_connect_kv` body.
pub assume_specification[begin_connect_kv_stub](state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_post_trivial(r);

/// Contract for `finish_connect_sql_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `finish_connect_sql` body.
pub assume_specification[finish_connect_sql_stub](state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_post_trivial(r);

/// Contract for `finish_connect_kv_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `finish_connect_kv` body.
pub assume_specification[finish_connect_kv_stub](state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_post_trivial(r);

/// Contract for `disconnect_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `disconnect` body.
pub assume_specification[disconnect_stub](state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_post_trivial(r);

/// Contract for `reconnect_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `reconnect` body.
pub assume_specification[reconnect_stub](state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_post_passthrough(state, r);

/// Contract for `connection_error_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `connection_error` body.
pub assume_specification[connection_error_stub](state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_post_trivial(r);

// ─── Verified exec callers ───────────────────────────────────────────────────────

// These exec fns close the proof loop: calling any stub on a consistent
// pre-state yields a consistent post-state, by the assume_specification.

/// Proof that `begin_connect_sql` preserves the invariant (via assume_specification).
pub fn begin_connect_sql_verified(state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_consistent(&r),
{
    begin_connect_sql_stub(state)
}

/// Proof that `begin_connect_kv` preserves the invariant (via assume_specification).
pub fn begin_connect_kv_verified(state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_consistent(&r),
{
    begin_connect_kv_stub(state)
}

/// Proof that `finish_connect_sql` preserves the invariant (via assume_specification).
pub fn finish_connect_sql_verified(state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_consistent(&r),
{
    finish_connect_sql_stub(state)
}

/// Proof that `finish_connect_kv` preserves the invariant (via assume_specification).
pub fn finish_connect_kv_verified(state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_consistent(&r),
{
    finish_connect_kv_stub(state)
}

/// Proof that `disconnect` preserves the invariant (via assume_specification).
pub fn disconnect_verified(state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_consistent(&r),
{
    disconnect_stub(state)
}

/// Proof that `reconnect` preserves the invariant (via assume_specification).
pub fn reconnect_verified(state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_consistent(&r),
{
    reconnect_stub(state)
}

/// Proof that `connection_error` preserves the invariant (via assume_specification).
pub fn connection_error_verified(state: ArchiveConnectionState) -> (r: ArchiveConnectionState)
    requires archive_connection_consistent(&state),
    ensures  archive_connection_consistent(&r),
{
    connection_error_stub(state)
}

// ─── Transition tags + composition ──────────────────────────────────────────────

/// One tag per `ArchiveConnectionMachine` transition — dispatch without re-verifying bodies.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ArchiveConnectionMachineTrans {
    BeginConnectSql,
    BeginConnectKv,
    FinishConnectSql,
    FinishConnectKv,
    Disconnect,
    Reconnect,
    ConnectionError,
}

/// Maps each transition tag to its postcondition predicate.
pub open spec fn archive_connection_post(pre: ArchiveConnectionState, post: ArchiveConnectionState, tag: ArchiveConnectionMachineTrans) -> bool {
    match tag {
        ArchiveConnectionMachineTrans::BeginConnectSql => archive_connection_post_trivial(post),
        ArchiveConnectionMachineTrans::BeginConnectKv => archive_connection_post_trivial(post),
        ArchiveConnectionMachineTrans::FinishConnectSql => archive_connection_post_trivial(post),
        ArchiveConnectionMachineTrans::FinishConnectKv => archive_connection_post_trivial(post),
        ArchiveConnectionMachineTrans::Disconnect => archive_connection_post_trivial(post),
        ArchiveConnectionMachineTrans::Reconnect => archive_connection_post_passthrough(pre, post),
        ArchiveConnectionMachineTrans::ConnectionError => archive_connection_post_trivial(post),
    }
}

/// Composition: any post-state produced by a tagged transition satisfies the invariant.
pub proof fn archive_connection_composition(pre: ArchiveConnectionState, post: ArchiveConnectionState, tag: ArchiveConnectionMachineTrans)
    requires
        archive_connection_consistent(&pre),
        archive_connection_post(pre, post, tag),
    ensures archive_connection_consistent(&post),
{
    match tag {
        ArchiveConnectionMachineTrans::BeginConnectSql => archive_connection_leaf_trivial(post),
        ArchiveConnectionMachineTrans::BeginConnectKv => archive_connection_leaf_trivial(post),
        ArchiveConnectionMachineTrans::FinishConnectSql => archive_connection_leaf_trivial(post),
        ArchiveConnectionMachineTrans::FinishConnectKv => archive_connection_leaf_trivial(post),
        ArchiveConnectionMachineTrans::Disconnect => archive_connection_leaf_trivial(post),
        ArchiveConnectionMachineTrans::Reconnect => archive_connection_leaf_passthrough(pre, post),
        ArchiveConnectionMachineTrans::ConnectionError => archive_connection_leaf_trivial(post),
    }
}

} // verus!
