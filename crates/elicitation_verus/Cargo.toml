[package]
name = "elicitation_verus"
version = "0.8.2"
edition = "2024"

[package.metadata.verus]
verify = true

[dependencies]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(verus)'] }
