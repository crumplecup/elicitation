//! `ArchiveDisplayPlugin` — render any archive descriptor to an AccessKit tree.
//!
//! Takes a descriptor type and a display mode string, invokes the
//! [`ArchiveDisplay`] trait, and returns a serialisable `Vec<NodeJson>` that
//! feeds directly into `elicit_ui` frontends.

// TODO(archive-display-plugin): implement ArchiveDisplayPlugin
