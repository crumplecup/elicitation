// AUTO-GENERATED by elicit_proofs/build.rs — DO NOT EDIT
// Regenerate: cargo build -p elicit_proofs
//
// Creusot companion contracts for ArchiveNavMachine.
// Source: elicit_server::archive::vsm

#[cfg(creusot)]
use ::creusot_std::prelude::*;
#[cfg(creusot)]
use elicit_server::archive::display::*;
#[cfg(creusot)]
use elicit_server::archive::nav_tree::*;
#[cfg(creusot)]
use elicit_server::archive::types::*;
#[cfg(creusot)]
use elicit_server::archive::vsm::*;
#[cfg(creusot)]
use elicitation::Established;
#[cfg(creusot)]
use elicitation::kani_label;
#[cfg(creusot)]
#[logic]
pub fn archive_nav_consistent(state: &ArchiveNavState) -> bool {
    pearlite! {
        match state { ArchiveNavState::NavFiltered { filter, .. } => filter @.len() > 0,
        _ => true, }
    }
}
#[cfg(creusot)]
#[requires(true)]
#[ensures(result)]
#[trusted]
pub fn verify_archive_nav_consistent_prop_creusot() -> bool {
    true
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&_state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn load_nav__creusot(
    _state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    (ArchiveNavState::NavLoading, proof)
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&_state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn nav_loaded__creusot(
    _state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
    nav: NavTree,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    let schemas = nav
        .schemas
        .into_iter()
        .map(|e| SchemaWithExpand {
            entry: e,
            expanded: false,
            functions_expanded: false,
            sequences_expanded: false,
            types_expanded: false,
            triggers_expanded: false,
        })
        .collect();
    (
        ArchiveNavState::NavReady {
            schemas,
            cursor: 0,
            filter: String::new(),
            filter_active: false,
            show_help: false,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&_state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn nav_refresh__creusot(
    _state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    (ArchiveNavState::NavLoading, proof)
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn expand_schema__creusot(
    state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
    schema_idx: usize,
    expanded: bool,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    let next = match state {
        ArchiveNavState::NavReady {
            mut schemas,
            cursor,
            filter,
            filter_active,
            show_help,
        } => {
            if let Some(s) = schemas.get_mut(schema_idx) {
                s.expanded = expanded;
            }
            ArchiveNavState::NavReady {
                schemas,
                cursor,
                filter,
                filter_active,
                show_help,
            }
        }
        other => other,
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn collapse_schema__creusot(
    state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
    schema_idx: usize,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    expand_schema__creusot(state, proof, schema_idx, false)
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn move_cursor_up__creusot(
    state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    let next = match state {
        ArchiveNavState::NavReady {
            schemas,
            cursor,
            filter,
            filter_active,
            show_help,
        } => ArchiveNavState::NavReady {
            schemas,
            cursor: cursor.saturating_sub(1),
            filter,
            filter_active,
            show_help,
        },
        other => other,
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn move_cursor_down__creusot(
    state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
    max: usize,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    let next = match state {
        ArchiveNavState::NavReady {
            schemas,
            cursor,
            filter,
            filter_active,
            show_help,
        } => ArchiveNavState::NavReady {
            schemas,
            cursor: cursor.saturating_add(1).min(max.saturating_sub(1)),
            filter,
            filter_active,
            show_help,
        },
        other => other,
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn apply_filter__creusot(
    state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
    filter: String,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    let next = match state {
        ArchiveNavState::NavReady { schemas, .. }
        | ArchiveNavState::NavFiltered { schemas, .. } => {
            if filter.is_empty() {
                ArchiveNavState::NavReady {
                    schemas,
                    cursor: 0,
                    filter: String::new(),
                    filter_active: true,
                    show_help: false,
                }
            } else {
                ArchiveNavState::NavFiltered {
                    schemas,
                    filter,
                    cursor: 0,
                }
            }
        }
        other => other,
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_nav_consistent(&state))]
#[ensures(archive_nav_consistent(&result.0))]
pub(crate) fn clear_filter__creusot(
    state: ArchiveNavState,
    proof: Established<ArchiveNavConsistent>,
) -> (ArchiveNavState, Established<ArchiveNavConsistent>) {
    apply_filter__creusot(state, proof, String::new())
}
