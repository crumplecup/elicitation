// AUTO-GENERATED by elicit_proofs/build.rs — DO NOT EDIT
// Regenerate: cargo build -p elicit_proofs
//
// Creusot companion contracts for ArchivePanelMachine.
// Source: elicit_server::archive::vsm

#[cfg(creusot)]
use ::creusot_std::prelude::*;
#[cfg(creusot)]
use elicitation::Established;
#[cfg(creusot)]
use elicitation::kani_label;
#[cfg(creusot)]
use elicit_server::archive::vsm::*;
#[cfg(creusot)]
use elicit_server::archive::types::*;
#[cfg(creusot)]
use elicit_server::archive::display::*;
#[cfg(creusot)]
use elicit_server::archive::nav_tree::*;
#[cfg(creusot)]
use crate::creusot::vsm_invariants::archive_panel_consistent;
#[cfg(creusot)]
#[requires(true)]
#[ensures(result)]
#[trusted]
pub fn verify_archive_panel_consistent_prop_creusot() -> bool {
    true
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn column_detail__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (ArchivePanelState::ColumnDetail, proof)
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn panel_loading__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    schema: String,
    label: String,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::Loading {
            schema,
            label,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn panel_error__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    message: String,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::ErrorView {
            message,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn data_grid_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    schema: String,
    table: String,
    query_result: QueryResult,
    display_mode: QueryResultMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::DataGrid {
            schema,
            table,
            result: query_result,
            page: 0,
            grid_row: 0,
            grid_col: 0,
            edit_state: None,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn query_complete__creusot(
    state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    query_result: QueryResult,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    let next = match state {
        ArchivePanelState::SqlEditor { text, error, .. } => {
            ArchivePanelState::SqlEditor {
                text,
                result: Some(query_result),
                running: false,
                error,
            }
        }
        other => other,
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn begin_edit__creusot(
    state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    let next = match state {
        ArchivePanelState::DataGrid {
            schema,
            table,
            result,
            page,
            grid_row,
            grid_col,
            display_mode,
            ..
        } => {
            ArchivePanelState::DataGrid {
                schema,
                table,
                result,
                page,
                grid_row,
                grid_col,
                edit_state: Some(RowEditState::default()),
                display_mode,
            }
        }
        other => other,
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn commit_edits__creusot(
    state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    let next = match state {
        ArchivePanelState::DataGrid {
            schema,
            table,
            result,
            page,
            grid_row,
            grid_col,
            display_mode,
            ..
        } => {
            ArchivePanelState::DataGrid {
                schema,
                table,
                result,
                page,
                grid_row,
                grid_col,
                edit_state: None,
                display_mode,
            }
        }
        other => other,
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn abort_edits__creusot(
    state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    commit_edits__creusot(state, proof)
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn open_sql_editor__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    initial_text: String,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::SqlEditor {
            text: initial_text,
            result: None,
            running: false,
            error: None,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn open_export_panel__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    schema: String,
    table: String,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::ExportView {
            schema,
            table,
            result: None,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn open_help_panel__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (ArchivePanelState::HelpView, proof)
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn open_saved_panel__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    entries: Vec<SavedQuery>,
    display_mode: SavedQueryMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::SavedView {
            entries,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn open_connection_editor__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    profile: ConnectionProfile,
    display_mode: ConnectionProfileMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::ConnectionEdit {
            profile: Box::new(profile),
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn ddl_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    schema: String,
    table: String,
    ddl: DdlDescriptor,
    display_mode: DdlDescriptorMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::DdlView {
            schema,
            table,
            ddl,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn explain_ready__creusot(
    state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    schema: String,
    table: String,
    root: ExplainPlan,
    display_mode: ExplainNodeMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    let next = match state {
        ArchivePanelState::ExplainView {
            schema: old_schema,
            table: old_table,
            root: old_root,
            ..
        } => {
            ArchivePanelState::ExplainCompare {
                schema: schema.clone(),
                table: table.clone(),
                comparison: ExplainComparison {
                    left: old_root,
                    right: root,
                    label_left: kani_label!("{old_schema}.{old_table}"),
                    label_right: kani_label!("{schema}.{table}"),
                },
            }
        }
        _ => {
            ArchivePanelState::ExplainView {
                schema,
                table,
                root,
                display_mode,
            }
        }
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn export_ready__creusot(
    state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    export_result: ExportResult,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    let next = match state {
        ArchivePanelState::ExportView { schema, table, .. } => {
            ArchivePanelState::ExportView {
                schema,
                table,
                result: Some(export_result),
            }
        }
        other => other,
    };
    (next, proof)
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn history_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    entries: Vec<QueryHistoryEntry>,
    display_mode: QueryHistoryEntryMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::HistoryView {
            entries,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn saved_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    entries: Vec<SavedQuery>,
    display_mode: SavedQueryMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::SavedView {
            entries,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn monitor_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    snapshot: MonitorSnapshot,
    display_mode: MonitorSnapshotMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::MonitorView {
            snapshot,
            loading: false,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn admin_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    snapshot: AdminSnapshot,
    display_mode: AdminSnapshotMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::AdminView {
            snapshot,
            loading: false,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn erd_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    schema: String,
    diagram: ErdDiagram,
    layout: Option<ErdLayout>,
    display_mode: ErdDiagramMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::ErdView {
            schema,
            diagram,
            layout,
            loading: false,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn constraints_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    schema: String,
    table: String,
    constraints: Vec<ConstraintDescriptor>,
    display_mode: ConstraintDescriptorMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::ConstraintView {
            schema,
            table,
            constraints,
            loading: false,
            display_mode,
        },
        proof,
    )
}
#[cfg(creusot)]
#[requires(archive_panel_consistent(&_state))]
#[ensures(archive_panel_consistent(&result.0))]
pub(crate) fn indexes_ready__creusot(
    _state: ArchivePanelState,
    proof: Established<ArchivePanelConsistent>,
    schema: String,
    table: String,
    indexes: Vec<IndexDescriptor>,
    display_mode: IndexDescriptorMode,
) -> (ArchivePanelState, Established<ArchivePanelConsistent>) {
    (
        ArchivePanelState::IndexView {
            schema,
            table,
            indexes,
            loading: false,
            display_mode,
        },
        proof,
    )
}
