// AUTO-GENERATED by `elicitation generate verus` — DO NOT EDIT
//
// Standalone Verus proof for ArchiveOverlayMachine (V13 assume_specification pattern).
// Regenerate: elicitation generate verus --crate-path <root>
//
// No imports from the VSM crate — self-contained for vargo builds.
// Architecture:
//   V11/V12 leaf + composition proof grounded by V13 assume_specification.
//   Each stub is trusted axiomatically (requires/ensures mirrors the
//   formal_method contract); Kani and Creusot independently verify the
//   real transition bodies in elicit_server.

use verus_builtin_macros::verus;
use vstd::prelude::*;

// ─── External transition stubs ─────────────────────────────────────────────────
// These functions represent the real transitions in elicit_server.
// Verus does not verify their bodies; assume_specification below injects
// the trusted contracts.  Kani/Creusot independently verify the real bodies.

/// Stub for `close_overlay` — body is opaque to Verus.
#[verifier::external]
pub fn close_overlay_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `open_help` — body is opaque to Verus.
#[verifier::external]
pub fn open_help_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `open_export_picker` — body is opaque to Verus.
#[verifier::external]
pub fn open_export_picker_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `picker_move_up` — body is opaque to Verus.
#[verifier::external]
pub fn picker_move_up_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `picker_move_down` — body is opaque to Verus.
#[verifier::external]
pub fn picker_move_down_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `open_save_prompt` — body is opaque to Verus.
#[verifier::external]
pub fn open_save_prompt_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `prompt_push` — body is opaque to Verus.
#[verifier::external]
pub fn prompt_push_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `prompt_backspace` — body is opaque to Verus.
#[verifier::external]
pub fn prompt_backspace_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `open_saved_browser` — body is opaque to Verus.
#[verifier::external]
pub fn open_saved_browser_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `saved_browser_up` — body is opaque to Verus.
#[verifier::external]
pub fn saved_browser_up_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

/// Stub for `saved_browser_down` — body is opaque to Verus.
#[verifier::external]
pub fn saved_browser_down_stub(state: ArchiveOverlayState) -> ArchiveOverlayState {
    todo!()
}

verus! {

/// Abstract mirror of `ArchiveOverlayState` (invariant-relevant variants only).
#[allow(unused_imports)]
use vstd::prelude::SpecOrd;

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum ArchiveOverlayState {
    ExportPickerOpen { idx: usize, len: usize },
    SavedBrowserOpen { len: usize, idx: usize },
    _Other,
}

/// Invariant for `ArchiveOverlayMachine`.
pub open spec fn archive_overlay_consistent(state: &ArchiveOverlayState) -> bool { match *state { ArchiveOverlayState::ExportPickerOpen { idx, len } => idx <= len, ArchiveOverlayState::SavedBrowserOpen { len, idx } => idx <= len, _ => true, } }

// ─── Postcondition predicates ──────────────────────────────────────────────────

/// Any non-special post-state — the `_ => true` arm of the invariant applies.
pub open spec fn archive_overlay_post_trivial(post: ArchiveOverlayState) -> bool { !(post matches ArchiveOverlayState::ExportPickerOpen { .. }) && !(post matches ArchiveOverlayState::SavedBrowserOpen { .. }) }

/// Passthrough: post-state equals pre-state.
pub open spec fn archive_overlay_post_passthrough(pre: ArchiveOverlayState, post: ArchiveOverlayState) -> bool { post == pre }

/// `ExportPickerOpen` post with non-violating field (invariant vacuously satisfied).
pub open spec fn archive_overlay_post_export_picker_open_false(post: ArchiveOverlayState) -> bool {
    match post {
        ArchiveOverlayState::ExportPickerOpen { idx, len } => idx <= len,
        _ => false,
    }
}

/// Conditional: `ExportPickerOpen` with non-violating field, or unchanged passthrough.
pub open spec fn archive_overlay_post_conditional_export_picker_open(pre: ArchiveOverlayState, post: ArchiveOverlayState) -> bool {
    match pre {
        ArchiveOverlayState::ExportPickerOpen { .. } => match post {
            ArchiveOverlayState::ExportPickerOpen { idx, len } => idx <= len,
            _ => false,
        },
        _ => post == pre,
    }
}

/// `SavedBrowserOpen` post with non-violating field (invariant vacuously satisfied).
pub open spec fn archive_overlay_post_saved_browser_open_false(post: ArchiveOverlayState) -> bool {
    match post {
        ArchiveOverlayState::SavedBrowserOpen { len, idx } => idx <= len,
        _ => false,
    }
}

/// Conditional: `SavedBrowserOpen` with non-violating field, or unchanged passthrough.
pub open spec fn archive_overlay_post_conditional_saved_browser_open(pre: ArchiveOverlayState, post: ArchiveOverlayState) -> bool {
    match pre {
        ArchiveOverlayState::SavedBrowserOpen { .. } => match post {
            ArchiveOverlayState::SavedBrowserOpen { len, idx } => idx <= len,
            _ => false,
        },
        _ => post == pre,
    }
}

// ─── Leaf lemmas ────────────────────────────────────────────────────────────────

/// Trivial post satisfies the invariant (`_ => true` arm applies).
pub proof fn archive_overlay_leaf_trivial(post: ArchiveOverlayState)
    requires archive_overlay_post_trivial(post),
    ensures  archive_overlay_consistent(&post),
{}

/// Passthrough preserves the invariant by induction.
pub proof fn archive_overlay_leaf_passthrough(pre: ArchiveOverlayState, post: ArchiveOverlayState)
    requires
        archive_overlay_consistent(&pre),
        archive_overlay_post_passthrough(pre, post),
    ensures archive_overlay_consistent(&post),
{}

/// `ExportPickerOpen` with non-violating field — invariant is vacuously satisfied.
pub proof fn archive_overlay_leaf_export_picker_open_false(post: ArchiveOverlayState)
    requires archive_overlay_post_export_picker_open_false(post),
    ensures  archive_overlay_consistent(&post),
{}

/// Conditional `ExportPickerOpen` — non-violating or passthrough — satisfies the invariant.
pub proof fn archive_overlay_leaf_conditional_export_picker_open(pre: ArchiveOverlayState, post: ArchiveOverlayState)
    requires
        archive_overlay_consistent(&pre),
        archive_overlay_post_conditional_export_picker_open(pre, post),
    ensures archive_overlay_consistent(&post),
{}

/// `SavedBrowserOpen` with non-violating field — invariant is vacuously satisfied.
pub proof fn archive_overlay_leaf_saved_browser_open_false(post: ArchiveOverlayState)
    requires archive_overlay_post_saved_browser_open_false(post),
    ensures  archive_overlay_consistent(&post),
{}

/// Conditional `SavedBrowserOpen` — non-violating or passthrough — satisfies the invariant.
pub proof fn archive_overlay_leaf_conditional_saved_browser_open(pre: ArchiveOverlayState, post: ArchiveOverlayState)
    requires
        archive_overlay_consistent(&pre),
        archive_overlay_post_conditional_saved_browser_open(pre, post),
    ensures archive_overlay_consistent(&post),
{}

// ─── Assume specifications (V13 trust anchors) ──────────────────────────────────

// Each assume_specification axiomatizes the contract for its stub.
// The postcondition predicate is the same one used by the leaf lemma above,
// ensuring Verus proof and assume_specification are tightly aligned.

/// Contract for `close_overlay_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `close_overlay` body.
pub assume_specification[close_overlay_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_trivial(r);

/// Contract for `open_help_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_help` body.
pub assume_specification[open_help_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_trivial(r);

/// Contract for `open_export_picker_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_export_picker` body.
pub assume_specification[open_export_picker_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_export_picker_open_false(r);

/// Contract for `picker_move_up_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `picker_move_up` body.
pub assume_specification[picker_move_up_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_conditional_export_picker_open(state, r);

/// Contract for `picker_move_down_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `picker_move_down` body.
pub assume_specification[picker_move_down_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_conditional_export_picker_open(state, r);

/// Contract for `open_save_prompt_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_save_prompt` body.
pub assume_specification[open_save_prompt_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_trivial(r);

/// Contract for `prompt_push_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `prompt_push` body.
pub assume_specification[prompt_push_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_passthrough(state, r);

/// Contract for `prompt_backspace_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `prompt_backspace` body.
pub assume_specification[prompt_backspace_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_passthrough(state, r);

/// Contract for `open_saved_browser_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `open_saved_browser` body.
pub assume_specification[open_saved_browser_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_trivial(r);

/// Contract for `saved_browser_up_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `saved_browser_up` body.
pub assume_specification[saved_browser_up_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_passthrough(state, r);

/// Contract for `saved_browser_down_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `saved_browser_down` body.
pub assume_specification[saved_browser_down_stub](state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_post_passthrough(state, r);

// ─── Verified exec callers ───────────────────────────────────────────────────────

// These exec fns close the proof loop: calling any stub on a consistent
// pre-state yields a consistent post-state, by the assume_specification.

/// Proof that `close_overlay` preserves the invariant (via assume_specification).
pub fn close_overlay_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    close_overlay_stub(state)
}

/// Proof that `open_help` preserves the invariant (via assume_specification).
pub fn open_help_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    open_help_stub(state)
}

/// Proof that `open_export_picker` preserves the invariant (via assume_specification).
pub fn open_export_picker_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    open_export_picker_stub(state)
}

/// Proof that `picker_move_up` preserves the invariant (via assume_specification).
pub fn picker_move_up_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    picker_move_up_stub(state)
}

/// Proof that `picker_move_down` preserves the invariant (via assume_specification).
pub fn picker_move_down_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    picker_move_down_stub(state)
}

/// Proof that `open_save_prompt` preserves the invariant (via assume_specification).
pub fn open_save_prompt_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    open_save_prompt_stub(state)
}

/// Proof that `prompt_push` preserves the invariant (via assume_specification).
pub fn prompt_push_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    prompt_push_stub(state)
}

/// Proof that `prompt_backspace` preserves the invariant (via assume_specification).
pub fn prompt_backspace_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    prompt_backspace_stub(state)
}

/// Proof that `open_saved_browser` preserves the invariant (via assume_specification).
pub fn open_saved_browser_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    open_saved_browser_stub(state)
}

/// Proof that `saved_browser_up` preserves the invariant (via assume_specification).
pub fn saved_browser_up_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    saved_browser_up_stub(state)
}

/// Proof that `saved_browser_down` preserves the invariant (via assume_specification).
pub fn saved_browser_down_verified(state: ArchiveOverlayState) -> (r: ArchiveOverlayState)
    requires archive_overlay_consistent(&state),
    ensures  archive_overlay_consistent(&r),
{
    saved_browser_down_stub(state)
}

// ─── Transition tags + composition ──────────────────────────────────────────────

/// One tag per `ArchiveOverlayMachine` transition — dispatch without re-verifying bodies.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum ArchiveOverlayMachineTrans {
    CloseOverlay,
    OpenHelp,
    OpenExportPicker,
    PickerMoveUp,
    PickerMoveDown,
    OpenSavePrompt,
    PromptPush,
    PromptBackspace,
    OpenSavedBrowser,
    SavedBrowserUp,
    SavedBrowserDown,
}

/// Maps each transition tag to its postcondition predicate.
pub open spec fn archive_overlay_post(pre: ArchiveOverlayState, post: ArchiveOverlayState, tag: ArchiveOverlayMachineTrans) -> bool {
    match tag {
        ArchiveOverlayMachineTrans::CloseOverlay => archive_overlay_post_trivial(post),
        ArchiveOverlayMachineTrans::OpenHelp => archive_overlay_post_trivial(post),
        ArchiveOverlayMachineTrans::OpenExportPicker => archive_overlay_post_export_picker_open_false(post),
        ArchiveOverlayMachineTrans::PickerMoveUp => archive_overlay_post_conditional_export_picker_open(pre, post),
        ArchiveOverlayMachineTrans::PickerMoveDown => archive_overlay_post_conditional_export_picker_open(pre, post),
        ArchiveOverlayMachineTrans::OpenSavePrompt => archive_overlay_post_trivial(post),
        ArchiveOverlayMachineTrans::PromptPush => archive_overlay_post_passthrough(pre, post),
        ArchiveOverlayMachineTrans::PromptBackspace => archive_overlay_post_passthrough(pre, post),
        ArchiveOverlayMachineTrans::OpenSavedBrowser => archive_overlay_post_trivial(post),
        ArchiveOverlayMachineTrans::SavedBrowserUp => archive_overlay_post_passthrough(pre, post),
        ArchiveOverlayMachineTrans::SavedBrowserDown => archive_overlay_post_passthrough(pre, post),
    }
}

/// Composition: any post-state produced by a tagged transition satisfies the invariant.
pub proof fn archive_overlay_composition(pre: ArchiveOverlayState, post: ArchiveOverlayState, tag: ArchiveOverlayMachineTrans)
    requires
        archive_overlay_consistent(&pre),
        archive_overlay_post(pre, post, tag),
    ensures archive_overlay_consistent(&post),
{
    match tag {
        ArchiveOverlayMachineTrans::CloseOverlay => archive_overlay_leaf_trivial(post),
        ArchiveOverlayMachineTrans::OpenHelp => archive_overlay_leaf_trivial(post),
        ArchiveOverlayMachineTrans::OpenExportPicker => archive_overlay_leaf_export_picker_open_false(post),
        ArchiveOverlayMachineTrans::PickerMoveUp => archive_overlay_leaf_conditional_export_picker_open(pre, post),
        ArchiveOverlayMachineTrans::PickerMoveDown => archive_overlay_leaf_conditional_export_picker_open(pre, post),
        ArchiveOverlayMachineTrans::OpenSavePrompt => archive_overlay_leaf_trivial(post),
        ArchiveOverlayMachineTrans::PromptPush => archive_overlay_leaf_passthrough(pre, post),
        ArchiveOverlayMachineTrans::PromptBackspace => archive_overlay_leaf_passthrough(pre, post),
        ArchiveOverlayMachineTrans::OpenSavedBrowser => archive_overlay_leaf_trivial(post),
        ArchiveOverlayMachineTrans::SavedBrowserUp => archive_overlay_leaf_passthrough(pre, post),
        ArchiveOverlayMachineTrans::SavedBrowserDown => archive_overlay_leaf_passthrough(pre, post),
    }
}

} // verus!
